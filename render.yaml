# Render Blueprint for funGame backend
# Deploy at: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: fungame-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 3001
      - key: PYTHONUNBUFFERED
        value: 1
